import { makeStyles } from "@material-ui/core/styles";

export const useVideoStyles = makeStyles({
  main: {
    width: "60%",
    height: "100%",
    marginLeft: "auto",
    marginRight: "auto",
    marginTop: "0%",
    marginBottom: "0%",
  },
});
